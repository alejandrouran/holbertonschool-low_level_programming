#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: 0
 */
int main(void)

{
	char pha;
	for (pha = 'a'; pha <= 'z'; pha++)
		putchar (pha);

	putchar('\n');
	return (0);
}
