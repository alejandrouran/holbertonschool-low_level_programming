#include <stdio.h>

/**
 * main - single digit numbers of base.
 *
 * Return: Always 0.
 */
int main(void)
{
	int nu;

	for (nu = '0'; nu <= '9'; nu++)
		putchar (nu);

        putchar (nu);

	return (0);
}
