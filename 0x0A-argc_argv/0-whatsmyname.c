#include <stdio.h>

/**
 * main - prints is name
 * @argc: number arguments
 * @argv: array arguments
 *
 * Return: Always 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{

	printf("%s. \n", argv[0]);

	return (0);
}
