#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: multiply the number a
 * @b: multiply the number b
 *
 * return: resuly of multiplicate first character for second character
*/

int mul(int a, int b)
{
	return (a * b);
}
